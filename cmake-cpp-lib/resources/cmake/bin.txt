# PLACEHOLDER-BEGIN #

#INSTALL(PROGRAMS  "${BINARY_OUTPUT_PATH}/xxx"                      DESTINATION bin)

IF(DISTRIBUTION STREQUAL "FreeBSD")
    #INSTALL(PROGRAMS  "${BINARY_OUTPUT_PATH}/xxx"                      DESTINATION bin)
ENDIF()

# PLACEHOLDER-END #
